<UserControl
    x:Class="ChatterBox.Client.Presentation.Shared.Views.WelcomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChatterBox.Client.Presentation.Shared.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid >
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Welcome" FontSize="24" HorizontalAlignment="Center" ></TextBlock>
            <StackPanel Margin="0,20" HorizontalAlignment="Center">
                <TextBlock Text="Name" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Name, Mode=TwoWay , UpdateSourceTrigger=PropertyChanged}" Width="150"/>
            </StackPanel>

            <StackPanel Margin="0,20" HorizontalAlignment="Center">
                <TextBlock Text="Domain" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Domain, Mode=TwoWay , UpdateSourceTrigger=PropertyChanged}" Width="150"/>
            </StackPanel>

            <Button Content="Next" Command="{Binding CompleteCommand}" HorizontalAlignment="Right" />

        </StackPanel>
    </Grid>
</UserControl>

<UserControl
    x:Class="ChatterBox.Client.Presentation.Shared.Views.RegistrationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChatterBox.Client.Win8._1.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel>
            <TextBlock Text="Hey, seems you're not connected ..." FontSize="24"></TextBlock>
            <StackPanel Orientation="Horizontal" Margin="0,20" HorizontalAlignment="Center">
                <TextBlock Text="Username" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Name, Mode=TwoWay , UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="10,0"/>
                <Button Content="Register" Command="{Binding CompleteRegistrationCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
